{
    "name": "PD",
    "dockerComposeFile": "../docker-compose.dev.yml", // 指定 compose 文件路径
    "service": "dev-0818", // 指定服务名，与你 compose 文件里的 service 名一致
    "workspaceFolder": "/app", // 容器内的工作目录，与你 compose 里的挂载一致
    "postCreateCommand": "npm install && npx prisma generate",
}