# 使用轻量基础镜像（已优化）
FROM node:22-alpine

# 设置工作目录（使用正斜杠）
WORKDIR /app

# 仅复制依赖文件（利用 Docker 缓存层）
COPY package*.json ./

# 安装依赖（开发依赖需保留）
RUN npm install

# 复制源代码（注意：Windows 路径需用正斜杠）
COPY . .

# 暴露开发端口
EXPOSE 3000

# 启动开发服务器（支持热重载）
CMD ["npm", "run", "dev"]